function searchRecipes() {
    const input = document.getElementById('search-bar').value.toLowerCase();
    const recipeLists = document.querySelectorAll('.recipe-list');
    
    recipeLists.forEach(recipeList => {
        const recipeItems = recipeList.querySelectorAll('li');
        let hasVisibleRecipe = false;
        
        recipeItems.forEach(recipe => {
            const recipeName = recipe.textContent.toLowerCase();
            if (recipeName.includes(input)) {
                recipe.style.display = '';
                hasVisibleRecipe = true;
            } else {
                recipe.style.display = 'none';
            }
        });
        
        recipeList.style.display = hasVisibleRecipe ? '' : 'none';
    });
} 